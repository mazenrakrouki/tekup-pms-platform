
%== It's advised to not modify the content of this file ===%
% To set your information, go to global_config.tex file    %
%==========================================================%

\thispagestyle{cover}%
\newgeometry{bottom=25mm,left=20mm,top=15mm,right=20mm}
% The three blocks of this row sit side by side: school logo, ministry heading,
% company logo, at 0.17 + 0.59 + 0.20 of the text width. They used to be declared
% at 0.2 + 0.6 + 0.02 + 0.2, which is 1.02 of the text
% width, which is 1.02 -- wider than the line -- so the last one wrapped and the
% ST2i logo printed underneath the TEK-UP one instead of opposite it.
%
% The two logos are set by height rather than by box width, because they are not
% the same kind of mark. ST2i is a wordmark, wide and heavy, and reads large at
% any size. TEK-UP is an abstract symbol with thin strokes, so matching their
% heights made it look like the smaller of the two. It is given 2.6cm against
% 1.9cm, which is what makes them look equal on the page rather than measure
% equal. tekup_trim.png is the same artwork with the white margin the original
% file carries on every edge removed, so this height is the height of the mark
% and not of the padding around it.
\noindent%
\begin{minipage}[c]{0.17\columnwidth}%
  \centering
  \includegraphics[height=2.6cm,keepaspectratio]{img/tekup_trim.png}%
\end{minipage}%
\hfill%
\begin{minipage}[c]{0.59\columnwidth}%
  \centering
  \footnotesize
  \textbf{Republic of Tunisia}\\[1.5mm]
  \textbf{Ministry of Higher Education\\
           and Scientific Research}\\[1.5mm]
  \textbf{Private Higher School of Engineering and Technology}\\[1.5mm]
  \textbf{TEK-UP}%
\end{minipage}%
\hfill%
\begin{minipage}[c]{0.20\columnwidth}%
  \centering
  \includegraphics[height=1.9cm,keepaspectratio]{img/st2i_logo.png}%
\end{minipage}

% The blocks below used to be separated by fixed skips, so they stacked from the
% top and every bit of slack pooled at the foot: the last line of the page sat at
% 63% of the height and the remaining 31% was blank. The skips are now elastic,
% written as \stretch, so the slack is shared between the blocks instead of
% falling to the bottom, and the page fills whatever height it is given. The
% weights are not equal: a little more is left under the supervisors than above
% the title, because a cover reads better when its centre of gravity sits just
% above the middle rather than exactly on it.
\vspace{\stretch{0.9}}

\begin{center}
{\huge{\textbf{\textsc{Final Year Project Report}}}}\\
\vskip6mm
\large
{\textbf{Submitted in partial fulfillment of the requirements for the}}\\
\vskip4mm
{\Large\textbf{\@diplomaName}}\\
\vskip3mm
{\textbf{Speciality: \@speciality}}
\end{center}

\vspace{\stretch{0.7}}

\begin{center}
\textrm{Prepared by}\\
\vskip4mm
{\ifthenelse{\boolean{isBinomal}}
    {\LARGE\textbf{\@author}~~~~~ et ~~~~~\textbf{\@secondAuthor}}
    {\LARGE\textbf{\@author}}
}
\end{center}

\vspace{\stretch{1.2}}

\definecolor{isiBlue}{RGB}{31, 78, 121}
% The rules used to be pulled 9mm into the left margin and set 1.1 times the text
% width, so the one element meant to anchor the page was the only one not aligned
% to it. They now span the measure exactly. The title itself is set in a narrower
% box than the rules: inset text reads as deliberate, and at this width the line
% breaks are chosen in global_config rather than left to chance: at this length
% an automatic break leaves a four-word line under three full ones.
\begin{center}
\begin{tcolorbox}[width=\columnwidth,colframe=isiBlue,colback=white,boxrule=0pt,
                 toprule=3pt,bottomrule=3pt,arc=0pt,top=0mm,right=0mm,left=0mm,
                 bottom=0mm,boxsep=0.5mm]{
  \begin{tcolorbox}[colframe=isiBlue,colback=white,boxrule=0pt,toprule=1pt,
                   bottomrule=1pt,arc=0pt,enlarge bottom by=-0.9mm,auto outer arc,
                   top=5mm,bottom=5mm]
    \centering
    \parbox{\columnwidth}{\centering\setstretch{1}%
      \huge\textbf{\@title}\par}
  \end{tcolorbox}
}
\end{tcolorbox}
\end{center}

\vspace{\stretch{1.0}}

% Three separate minipages meant three independent left edges, so the colons, the
% names and the titles each started somewhere different. One tabular aligns them:
% the labels are set flush right so their colons line up, and the two columns
% beside them start on a single edge each.
\begin{center}
\large
\begin{tabular}{@{}r@{\hspace{5mm}}l@{\hspace{10mm}}l@{}}
Industrial Supervisor: & \textbf{\@proFramerName}  & \@proFramerSpeciality \\[1.2mm]
Academic Supervisor: & \textbf{\@academicFramerName} & \@academicFramerSpeciality
\end{tabular}
\end{center}

\vspace{\stretch{1.3}}

\afterpage{\blankpage}